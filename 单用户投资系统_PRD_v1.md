# 单用户投资系统 PRD（Markdown 版）

- 文档版本：v1.0
- 更新时间：2026-04-03
- 产品类型：单用户、自用型投资分析与决策支持系统
- 目标用户：1 人（系统所有者）
- 技术栈约束：Python + SQLite + 简单 Web 前端

---

## 1. 产品概述

### 1.1 产品名称

单用户投资系统

### 1.2 产品定位

本系统是一个面向个人投资者的、自用型基金 / ETF / LOF 投资管理系统。系统不以高频自动交易为目标，而以“持仓管理、策略执行、风险控制、回测验证、复盘分析”为核心。

系统的核心任务是：

1. 管理当前持仓与历史交易。
2. 将长期账户与短期账户分开管理。
3. 基于预先定义的规则，对基金、ETF、LOF 进行评估。
4. 判断当前资产是否处于“价值洼地”、是否应加仓、减仓、暂停追加或观察。
5. 对策略进行回测与复盘，避免经验主义与情绪化操作。
6. 以简单、稳定、低维护成本为优先，而非复杂炫技。

### 1.3 产品目标

本系统的目标不是替代人的投资判断，而是将人的投资逻辑结构化、规则化、可追踪化，使投资决策具备以下特征：

- 可重复：同样的市场状态下，系统能够给出同样的建议。
- 可解释：每一次建议都能追溯到明确规则与原因。
- 可回测：策略上线前可以在历史数据上验证效果。
- 可维护：新增基金、清仓基金、修改参数都不需要大规模改代码。
- 可扩展：后续可逐步增加因子、策略模板和数据源。

### 1.4 非目标

本系统当前阶段**不**追求以下目标：

- 不做自动下单。
- 不做多用户权限系统。
- 不做社交、讨论、分享功能。
- 不做复杂券商接入。
- 不做实时毫秒级行情处理。
- 不做多市场高频交易系统。

---

## 2. 业务背景与问题定义

当前个人投资中存在以下典型问题：

1. 长期投资与短线交易混在一起，导致持仓角色不清晰。
2. 基金、ETF、LOF 名称不同，但底层资产高度重叠，造成“看似分散、实际集中”。
3. 补仓往往基于“摊低成本”而非“估值、配置、趋势与风控条件”。
4. 交易记录与持仓变化没有系统化留痕，难以复盘。
5. 新增基金或清仓基金后，策略配置容易混乱。
6. 回测、实盘、复盘彼此割裂，无法持续迭代。

因此，本系统需要解决的核心问题是：

- 如何用统一的数据结构描述基金、ETF、LOF。
- 如何将长期账户与短期账户在逻辑上隔离。
- 如何用模板化策略规则驱动投资建议。
- 如何在新增基金、清仓基金时保持系统稳定。
- 如何用低复杂度架构支撑持续优化。

---

## 3. 用户画像

### 3.1 用户身份

- 单一用户：系统所有者
- 具备基础投资知识
- 愿意使用规则系统约束自身操作
- 能接受月度复盘、周度执行、日度观察
- 对技术系统有一定理解，希望后续可自行维护或扩展

### 3.2 用户需求

用户希望系统能够：

- 录入并管理自己的基金 / ETF / LOF 持仓。
- 将账户区分为长期配置账户与短期轮动账户。
- 根据预设策略给出买入、减仓、止损、止盈、观察等建议。
- 对每个建议给出清晰原因。
- 对每只产品记录生命周期：在持、观察、已清仓、停用。
- 回顾历史表现，查看策略是否有效。
- 后续可继续增加新基金，而不破坏现有系统。

---

## 4. 产品范围

### 4.1 当前版本范围（V1）

V1 聚焦以下核心能力：

1. 产品管理
2. 账户管理
3. 持仓管理
4. 交易记录管理
5. 行情/净值数据管理
6. 策略模板管理
7. 策略信号计算
8. 投资建议展示
9. 回测功能
10. 复盘与日志
11. Web 端基础界面

### 4.2 后续版本可扩展范围

- 多策略对比
- 邮件/消息提醒
- 自动抓取基金净值和行情
- 图表增强
- 更丰富的因子模型
- 策略参数调优工具
- 导出报告（PDF/Markdown/HTML）

---

## 5. 核心设计原则

### 5.1 单用户优先

由于只有 1 个用户，系统设计上应尽量避免不必要的用户体系、权限体系、复杂认证和分布式架构。

### 5.2 数据优先于界面

优先把数据模型、策略结构、日志追踪做好，界面保持简单清晰。

### 5.3 策略模板化

不要把规则散落在各个页面和代码分支中，而应该抽象成模板与参数。

### 5.4 历史不可丢失

清仓不等于删除；任何产品与交易都要保留历史记录。

### 5.5 可解释优先于“智能”

每一个建议都必须能说明“为什么”，原因要可追溯。

### 5.6 简单部署优先

首选 Python + SQLite + 简单 Web，保证本地即可部署与运行。

---

## 6. 业务模型

### 6.1 账户模型

系统内逻辑上存在两个账户：

#### 6.1.1 核心配置账户（Core Account）

- 用途：长期投资、定投、资产配置、月度再平衡
- 适合资产：黄金 ETF、标普 500、纳指 100、全球主动基金等
- 规则特点：低频操作、重配置、少止损、重评分与权重偏离

#### 6.1.2 战术轮动账户（Tactical Account）

- 用途：主题轮动、趋势交易、主动风控
- 适合资产：AI、软件、半导体等高弹性主题资产
- 规则特点：高于长期账户的敏感度、更严格止损、更强调趋势确认

### 6.2 产品模型

系统支持以下产品类型：

- 场外基金（Mutual Fund）
- ETF
- LOF
- 现金（虚拟资产，用于配置缓冲）

### 6.3 产品生命周期状态

每个产品在系统中的状态包括：

- `watchlist`：观察中，未持仓
- `active`：启用中，可交易，可评估
- `paused`：暂停使用，不生成新信号
- `closed`：已清仓，保留历史
- `archived`：归档，通常用于历史分析

---

## 7. 功能需求

## 7.1 产品管理模块

### 7.1.1 功能目标

管理基金、ETF、LOF 的基础信息，支持新增、编辑、停用、清仓后保留。

### 7.1.2 功能点

1. 新增产品
2. 编辑产品信息
3. 指定产品类型
4. 指定产品归属账户
5. 指定交易方式（场内 / 场外）
6. 指定是否可定投
7. 指定是否启用
8. 产品列表展示
9. 历史归档

### 7.1.3 字段建议

- 产品代码
- 产品名称
- 产品类型
- 市场
- 交易方式
- 所属账户类型
- 所属策略模板
- 当前状态
- 备注

### 7.1.4 业务规则

- 新增产品时必须指定账户类型和策略模板。
- 删除产品采用软删除或状态变更，不做物理删除。
- 已有历史交易记录的产品不得直接物理删除。

---

## 7.2 账户管理模块

### 7.2.1 功能目标

在系统内管理逻辑账户，用于隔离长期投资与短期交易。

### 7.2.2 功能点

1. 查看账户列表
2. 查看账户总资产
3. 查看账户持仓分布
4. 查看账户收益率
5. 查看账户最大回撤
6. 账户参数设置

### 7.2.3 业务规则

- V1 只支持两个内置账户：`core`、`tactical`。
- 新增持仓必须归属于某一个账户。
- 不允许同一持仓记录同时归属两个账户。

---

## 7.3 持仓管理模块

### 7.3.1 功能目标

记录当前持仓状态，并计算成本、市值、浮动盈亏、权重等信息。

### 7.3.2 功能点

1. 持仓列表
2. 持仓详情
3. 当前成本价
4. 当前市值
5. 浮动盈亏
6. 账户内权重
7. 组合内权重
8. 持仓状态更新

### 7.3.3 业务规则

- 当前持仓应由交易记录推导，也允许手工修正。
- 若持仓数量为 0，则状态可变更为 `closed`。
- 清仓后仍保留持仓历史快照。

---

## 7.4 交易记录模块

### 7.4.1 功能目标

记录每一次买入、卖出、申购、赎回、定投、调仓等操作。

### 7.4.2 功能点

1. 手工录入交易
2. 查看交易流水
3. 按产品筛选
4. 按账户筛选
5. 按时间筛选
6. 展示交易原因
7. 支持导入历史交易

### 7.4.3 交易类型

- buy
- sell
- subscribe
- redeem
- rebalance_buy
- rebalance_sell
- dca_buy
- stop_loss_sell
- take_profit_sell
- manual_adjust

### 7.4.4 业务规则

- 每一笔交易都必须有时间、产品、账户、金额、方向。
- 建议为每笔交易记录 `reason_code`。
- 不允许无产品、无账户的孤立交易记录。

---

## 7.5 市场数据模块

### 7.5.1 功能目标

存储基金净值、ETF/LOF 行情、技术指标和辅助因子，用于策略计算与回测。

### 7.5.2 数据类型

- 开盘价
- 收盘价
- 最高价
- 最低价
- 成交量
- 单位净值
- 累计净值
- 均线
- 回撤指标
- 相对强弱指标

### 7.5.3 V1 要求

- 支持手工导入 CSV
- 支持通过 Python 脚本定期写入 SQLite
- 不强制要求实时行情

### 7.5.4 业务规则

- 场外基金可只保留净值数据。
- 场内 ETF / LOF 应尽量保留 OHLCV 数据。
- 策略计算前必须校验数据完整性。

---

## 7.6 策略模板模块

### 7.6.1 功能目标

将投资策略从“写死的代码”抽象为模板 + 参数，便于新增基金或调整逻辑。

### 7.6.2 建议策略模板

1. `core_index_template`
   - 用于标普 500、纳指 100 等宽基
2. `core_active_fund_template`
   - 用于主动型全球成长基金
3. `gold_hedge_template`
   - 用于黄金 ETF
4. `tactical_theme_template`
   - 用于 AI、软件、半导体等主题资产

### 7.6.3 模板参数示例

- 目标权重下限
- 目标权重上限
- 是否允许定投
- 加仓阈值
- 止损阈值
- 止盈阈值
- 月度评分阈值
- 趋势确认条件
- 是否参与再平衡

### 7.6.4 业务规则

- 每个产品必须绑定一个策略模板。
- 模板负责默认规则，产品级参数负责个性化覆盖。
- 修改模板后应保留变更记录。

---

## 7.7 策略信号模块

### 7.7.1 功能目标

根据市场数据、持仓状态和策略模板，自动生成建议信号。

### 7.7.2 核心信号类型

- allow_buy
- allow_add
- hold
- reduce
- take_profit
- stop_loss
- suspend_add
- observe
- rebalance

### 7.7.3 核心账户信号逻辑

核心账户信号以“配置优先”为原则，主要基于：

- 当前权重 vs 目标权重
- 月度评分
- 回撤区间
- 趋势状态
- 产品质量（基金经理变化、风格漂移、费率等）

### 7.7.4 战术账户信号逻辑

战术账户信号以“趋势优先”为原则，主要基于：

- 20 日均线
- 60 日均线
- 趋势方向
- 买入后收益率
- 持仓高点回撤
- 组合级回撤

### 7.7.5 业务规则

- 同一时点每个产品最多输出 1 个主信号。
- 所有信号必须带原因码与解释文本。
- 若数据缺失，系统应输出“无法评估”而非静默失败。

---

## 7.8 价值洼地评分模块

### 7.8.1 功能目标

对核心账户资产进行定期打分，用于判断是否具备新增资金配置价值。

### 7.8.2 评分维度

1. 回撤分
2. 配置缺口分
3. 趋势分
4. 产品质量分

### 7.8.3 评分规则示例

总分 100 分：

- 回撤分：40 分
- 配置缺口分：20 分
- 趋势分：20 分
- 产品质量分：20 分

### 7.8.4 评分结果分级

- `>= 70`：允许新增资金
- `55 ~ 69`：持有观察
- `< 55`：暂停新增

### 7.8.5 业务规则

- 价值洼地评分只对核心账户资产启用。
- 战术账户不使用该评分作为入场依据。

---

## 7.9 再平衡模块

### 7.9.1 功能目标

当组合权重偏离目标区间时，提示用户用新增资金或减仓方式进行再平衡。

### 7.9.2 再平衡触发条件

- 月度检查时发现某资产相对目标中位权重偏离超过阈值
- 阈值建议：20%

### 7.9.3 再平衡方式

1. 优先用新增资金纠偏
2. 若偏离过大且新增资金不足，再考虑部分减仓

### 7.9.4 业务规则

- 再平衡优先针对核心账户。
- 战术账户以止损/止盈/趋势退出为主，不使用常规配置再平衡逻辑。

---

## 7.10 回测模块

### 7.10.1 功能目标

基于历史数据验证策略表现，评估收益、回撤、胜率、换手率等指标。

### 7.10.2 功能点

1. 选择时间区间
2. 选择账户或策略模板
3. 运行回测
4. 输出绩效指标
5. 查看交易过程
6. 参数对比
7. Walk-forward 验证

### 7.10.3 回测指标

- 年化收益率
- 最大回撤
- 年化波动率
- Calmar Ratio
- 胜率
- 盈亏比
- 换手率
- 平均持有天数
- 最大连续亏损次数

### 7.10.4 业务规则

- 回测结果必须与实盘规则使用同一套信号逻辑。
- 不允许仅根据全样本最优参数直接上线。
- V1 应至少支持简单参数组对比。

---

## 7.11 日志与复盘模块

### 7.11.1 功能目标

记录系统信号、参数变更、交易原因和人工备注，支撑长期复盘。

### 7.11.2 日志类型

- 策略信号日志
- 参数变更日志
- 手工操作日志
- 错误日志
- 数据导入日志

### 7.11.3 复盘维度

- 哪些建议被执行了
- 哪些建议未被执行
- 执行后的效果如何
- 哪些亏损来自规则外操作
- 哪些收益来自策略本身

---

## 8. 页面需求（简单 Web）

V1 前端以“简单清晰、快速可用”为原则。推荐使用：

- Flask + Jinja2 模板
- 或 FastAPI + 简单模板
- 或 Streamlit（如果偏工具型）

若强调“简单 Web”，优先建议：

- 后端：Flask
- 前端：Jinja2 + Bootstrap

## 8.1 页面总览

建议页面如下：

1. 首页 / 仪表盘
2. 产品管理页
3. 持仓页
4. 交易记录页
5. 策略建议页
6. 回测页
7. 参数配置页
8. 日志与复盘页

---

## 8.2 首页 / 仪表盘

### 展示内容

- 总资产
- 核心账户总资产
- 战术账户总资产
- 当日盈亏 / 累计盈亏
- 当前持仓数量
- 需要关注的信号数量
- 风险提示
- 最近交易记录

### 交互要求

- 一眼看到整体状态
- 可点击进入具体模块

---

## 8.3 产品管理页

### 展示内容

- 产品代码
- 产品名称
- 产品类型
- 所属账户
- 所属策略模板
- 状态
- 操作按钮（编辑 / 停用 / 归档）

### 支持操作

- 新增产品
- 编辑产品
- 状态变更

---

## 8.4 持仓页

### 展示内容

- 所属账户
- 产品名称
- 持仓份额
- 成本价
- 当前价 / 净值
- 当前市值
- 浮盈浮亏
- 组合权重
- 当前信号

### 交互要求

- 支持按账户筛选
- 支持查看产品详情页

---

## 8.5 交易记录页

### 展示内容

- 时间
- 产品
- 账户
- 方向
- 金额
- 价格 / 净值
- 手续费
- 原因码
- 备注

### 支持操作

- 手工录入交易
- 编辑备注
- 按条件筛选

---

## 8.6 策略建议页

### 展示内容

- 产品
- 当前信号
- 建议动作
- 原因说明
- 所属账户
- 优先级
- 风险提示
- 最近评分 / 趋势状态

### 交互要求

- 支持“仅看需要处理的建议”
- 支持查看建议历史

---

## 8.7 回测页

### 展示内容

- 回测时间区间
- 所选策略模板
- 参数集
- 收益曲线
- 回撤曲线
- 指标统计表
- 交易明细

### 支持操作

- 启动回测
- 对比参数组
- 导出结果

---

## 8.8 参数配置页

### 展示内容

- 各策略模板参数
- 当前启用参数
- 最近修改时间

### 支持操作

- 修改模板参数
- 产品级覆盖
- 保存并记录版本

---

## 8.9 日志与复盘页

### 展示内容

- 信号日志
- 数据更新日志
- 参数变更日志
- 手工干预日志
- 复盘备注

---

## 9. 数据库设计（SQLite）

SQLite 非常适合当前阶段，原因如下：

- 单用户场景匹配度高
- 零运维
- 文件级部署简单
- 适合本地 Web 系统
- 对当前数据规模完全足够

## 9.1 数据库核心表

### 9.1.1 accounts

用于存储逻辑账户信息。

字段建议：

- `id` INTEGER PK
- `account_code` TEXT UNIQUE
- `account_name` TEXT
- `account_type` TEXT  // core / tactical
- `status` TEXT
- `created_at` DATETIME
- `updated_at` DATETIME

### 9.1.2 instruments

用于存储产品主数据。

字段建议：

- `id` INTEGER PK
- `symbol` TEXT UNIQUE
- `name` TEXT
- `instrument_type` TEXT  // fund / etf / lof / cash
- `market` TEXT
- `trade_mode` TEXT  // exchange_traded / eod_nav
- `default_account_type` TEXT
- `default_strategy_template` TEXT
- `status` TEXT
- `notes` TEXT
- `created_at` DATETIME
- `updated_at` DATETIME

### 9.1.3 strategy_templates

用于存储策略模板。

字段建议：

- `id` INTEGER PK
- `template_code` TEXT UNIQUE
- `template_name` TEXT
- `account_type` TEXT
- `template_type` TEXT
- `config_json` TEXT
- `version` TEXT
- `status` TEXT
- `created_at` DATETIME
- `updated_at` DATETIME

### 9.1.4 strategy_assignments

用于将产品绑定到某个策略模板。

字段建议：

- `id` INTEGER PK
- `instrument_id` INTEGER
- `account_id` INTEGER
- `template_id` INTEGER
- `target_weight_lower` REAL
- `target_weight_upper` REAL
- `allow_dca` INTEGER
- `allow_rebalance` INTEGER
- `custom_config_json` TEXT
- `status` TEXT
- `created_at` DATETIME
- `updated_at` DATETIME

### 9.1.5 positions

用于存储当前持仓快照。

字段建议：

- `id` INTEGER PK
- `account_id` INTEGER
- `instrument_id` INTEGER
- `quantity` REAL
- `avg_cost` REAL
- `market_price` REAL
- `market_value` REAL
- `unrealized_pnl` REAL
- `weight_in_account` REAL
- `position_status` TEXT
- `updated_at` DATETIME

### 9.1.6 trades

用于存储交易流水。

字段建议：

- `id` INTEGER PK
- `account_id` INTEGER
- `instrument_id` INTEGER
- `trade_date` DATE
- `trade_type` TEXT
- `side` TEXT
- `quantity` REAL
- `price` REAL
- `amount` REAL
- `fee` REAL
- `reason_code` TEXT
- `notes` TEXT
- `created_at` DATETIME

### 9.1.7 market_data

用于存储行情与净值。

字段建议：

- `id` INTEGER PK
- `instrument_id` INTEGER
- `trade_date` DATE
- `open` REAL
- `high` REAL
- `low` REAL
- `close` REAL
- `volume` REAL
- `nav` REAL
- `ma20` REAL
- `ma60` REAL
- `ma120` REAL
- `drawdown_60d` REAL
- `relative_strength_20d` REAL
- `created_at` DATETIME

### 9.1.8 signals

用于存储系统生成的建议信号。

字段建议：

- `id` INTEGER PK
- `signal_date` DATE
- `account_id` INTEGER
- `instrument_id` INTEGER
- `signal_type` TEXT
- `priority` INTEGER
- `reason_code` TEXT
- `explanation` TEXT
- `score` REAL
- `risk_flag` TEXT
- `status` TEXT
- `created_at` DATETIME

### 9.1.9 backtest_runs

用于存储回测任务与摘要结果。

字段建议：

- `id` INTEGER PK
- `run_name` TEXT
- `template_id` INTEGER
- `start_date` DATE
- `end_date` DATE
- `params_json` TEXT
- `result_json` TEXT
- `created_at` DATETIME

### 9.1.10 system_logs

用于存储系统事件日志。

字段建议：

- `id` INTEGER PK
- `log_type` TEXT
- `level` TEXT
- `message` TEXT
- `context_json` TEXT
- `created_at` DATETIME

## 9.2 数据库设计要求

- 所有表都应有主键。
- 所有外键关系应在应用层做严格校验。
- 对 `symbol`、`account_code`、`template_code` 建唯一约束。
- 对高频查询字段建索引，例如：
  - `market_data(instrument_id, trade_date)`
  - `trades(account_id, trade_date)`
  - `signals(signal_date, account_id)`

---

## 10. 后端架构设计（Python）

## 10.1 推荐技术选型

### 后端框架

- Flask：更轻量，更适合当前需求
- 若后续想做 API-first，可选 FastAPI

V1 推荐：**Flask + SQLAlchemy + Jinja2**

### 数据层

- SQLite
- SQLAlchemy ORM
- Alembic（如需后续迁移）

### 数据分析与计算

- pandas
- numpy
- 适度使用 ta / 自定义指标函数

### 图表

- ECharts 或 Chart.js
- 简单版本也可先用表格 + 少量折线图

---

## 10.2 后端模块划分

建议按模块拆分：

- `app/`
  - `models/`：ORM 模型
  - `services/`：业务逻辑
  - `repositories/`：数据库读写
  - `strategies/`：策略模板与计算逻辑
  - `backtest/`：回测逻辑
  - `views/`：Web 路由
  - `templates/`：HTML 模板
  - `static/`：静态资源
  - `utils/`：工具函数

---

## 10.3 核心服务建议

### 10.3.1 InstrumentService

负责：

- 新增产品
- 更新产品
- 启停产品
- 查询产品列表

### 10.3.2 PositionService

负责：

- 根据交易更新持仓
- 计算持仓成本
- 计算权重和盈亏

### 10.3.3 MarketDataService

负责：

- 导入行情 / 净值
- 计算均线与指标
- 数据完整性检查

### 10.3.4 SignalService

负责：

- 读取持仓、策略模板、行情数据
- 生成每日 / 每月信号
- 输出解释和原因码

### 10.3.5 RebalanceService

负责：

- 计算目标权重偏离
- 生成再平衡建议

### 10.3.6 BacktestService

负责：

- 加载历史数据
- 执行策略逻辑
- 统计回测结果

### 10.3.7 ReportService

负责：

- 生成复盘摘要
- 导出 Markdown / HTML / CSV 报告

---

## 11. 策略执行逻辑

## 11.1 核心账户逻辑

### 输入

- 当前权重
- 目标权重区间
- 月度评分
- 回撤情况
- 产品质量信息

### 输出

- 允许新增
- 持有
- 暂停新增
- 再平衡

### 核心逻辑原则

- 以配置为先
- 以新增资金纠偏为先
- 不做频繁止损
- 重视长期评分与配置偏离

---

## 11.2 战术账户逻辑

### 输入

- 当前价格 / 净值
- MA20 / MA60
- 收益率
- 高点回撤
- 账户级回撤

### 输出

- 试探买入
- 确认加仓
- 减仓
- 止损
- 止盈
- 暂停开仓

### 核心逻辑原则

- 趋势确认优先
- 严格止损
- 分级止盈
- 控制主题集中度

---

## 11.3 推荐初版规则（可配置）

### 核心账户

- 月度检查一次
- 偏离目标权重超过 20% 触发再平衡
- 新增资金优先补低配资产
- 价值洼地评分 >= 70 才允许新增

### 战术账户

- 站上 MA20 且 MA20 向上时可试错
- 盈利 4%~5% 后可加满计划仓
- 回撤 8% 先减半
- 回撤 10% 清仓
- 盈利 15% 止盈 1/3
- 盈利 25% 再止盈 1/3
- 剩余仓位使用移动止盈

---

## 12. 非功能需求

## 12.1 性能

- 单用户场景下，页面响应应尽量控制在 1~2 秒内。
- 普通数据量下，SQLite 足以支撑。
- 单次回测若数据不大，应在可接受时间内完成。

## 12.2 可维护性

- 代码分层清晰
- 配置与代码分离
- 策略模板可扩展
- 表结构尽量稳定

## 12.3 可用性

- 页面清晰、少跳转、少嵌套
- 关键页面支持搜索与筛选
- 错误提示明确

## 12.4 安全性

虽然是单用户系统，仍建议具备基本安全措施：

- 本地管理员密码
- 表单校验
- 防止 SQL 注入
- 数据库文件定期备份

## 12.5 可移植性

- 支持本地开发环境快速启动
- 支持部署到个人服务器 / NAS / 本地电脑

---

## 13. 部署需求

### 13.1 部署方式

建议支持以下两种：

1. 本地运行
2. 轻量服务器运行

### 13.2 基本要求

- Python 3.11+
- SQLite 3+
- pip / uv / poetry 任选其一
- 可通过 `python app.py` 或 `flask run` 启动

### 13.3 目录建议

```text
project/
  app/
  data/
    app.db
    imports/
  scripts/
  tests/
  requirements.txt
  README.md
```

---

## 14. 开发优先级

## 14.1 P0（必须有）

- SQLite 数据模型
- 产品管理
- 账户管理
- 持仓管理
- 交易记录管理
- 行情导入
- 基础策略信号
- 策略建议页
- 基础回测

## 14.2 P1（应该有）

- 参数配置页
- 日志与复盘
- 图表展示
- 报告导出

## 14.3 P2（可以后续做）

- 自动抓取数据
- 邮件提醒
- 参数搜索
- 更多可视化

---

## 15. 验收标准

### 15.1 数据管理验收

- 能新增基金 / ETF / LOF
- 能清仓但不丢失历史
- 能按账户查看持仓
- 能录入和查看交易历史

### 15.2 策略功能验收

- 能根据不同账户类型生成不同建议
- 能输出原因码和解释
- 能计算价值洼地评分
- 能识别再平衡机会
- 能执行基础回测

### 15.3 前端验收

- 页面可正常访问
- 核心路径清晰
- 表格信息准确
- 基本交互流畅

### 15.4 系统稳定性验收

- 数据导入失败时有明确提示
- 信号计算失败时不应导致整个页面崩溃
- 修改参数后应保留历史记录

---

## 16. 风险与限制

### 16.1 业务风险

- 规则过于简单时，可能失真。
- 规则过于复杂时，可能过拟合。
- 基金净值与场内价格的差异会影响策略一致性。

### 16.2 技术风险

- SQLite 在当前规模足够，但后续若数据过大、并发增加，可能需要迁移。
- 手工导入数据容易出现脏数据。
- 若策略与数据库绑定过深，后续扩展难度会上升。

### 16.3 使用风险

- 用户可能手工操作偏离系统建议，导致策略评估失真。
- 若长期不维护产品状态、模板参数，系统可能逐渐失准。

---

## 17. 版本规划建议

## V1：最小可用版本

目标：能记录、能评估、能建议、能回测。

包含：

- 基础数据库
- Web 页面
- 核心 / 战术账户
- 产品与持仓管理
- 策略信号
- 基础回测

## V2：增强版

目标：更可维护、更可复盘。

包含：

- 参数版本管理
- 日志复盘增强
- 图表增强
- 数据导入优化

## V3：进阶版

目标：更自动化、更智能。

包含：

- 自动数据同步
- 提醒系统
- 多策略比较
- 更复杂因子

---

## 18. MVP 建议

若希望尽快落地，建议先做最小版本：

1. SQLite 表结构搭好
2. 完成产品、账户、持仓、交易四个基础模块
3. 完成市场数据导入
4. 完成核心/战术两套信号逻辑
5. 完成策略建议页面
6. 完成一个简单回测页

只要这 6 步完成，系统就已经具备实际使用价值。

---

## 19. 结论

这是一个面向单用户的、规则驱动的投资管理系统。它最重要的不是“预测市场”，而是：

- 把长期账户和短期账户分开
- 把基金、ETF、LOF 统一纳入同一套产品模型
- 把新增基金与清仓基金变成可维护的数据操作
- 把投资逻辑转成可追踪、可复盘、可回测的策略系统

在“只有 1 个用户、使用 Python、数据库使用 SQLite、前端使用简单 Web”的前提下，以上设计足够完善，也足够务实，适合作为你的第一个可落地版本。

---

## 20. 附录：建议的初始策略模板

### 20.1 core_index_template

适用：标普 500、纳指 100

主要参数：

- 目标权重区间
- 三档补仓阈值
- 月度评分阈值
- 再平衡阈值

### 20.2 core_active_fund_template

适用：全球成长主动基金

主要参数：

- 定投频率
- 定投金额
- 基金经理变更监控
- 风格漂移监控
- 月度评分阈值

### 20.3 gold_hedge_template

适用：黄金 ETF

主要参数：

- 目标权重区间
- 是否允许主动加仓
- 是否仅作平衡器

### 20.4 tactical_theme_template

适用：AI、软件、半导体等主题资产

主要参数：

- MA20 / MA60 条件
- 初始仓位比例
- 确认加仓条件
- 止损阈值
- 止盈阈值
- 移动止盈阈值
- 账户级回撤防御阈值

